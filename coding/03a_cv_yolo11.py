# -*- coding: utf-8 -*-
"""03a_CV_yolo11.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/114fr84d4TjoaV6y0tYbgeKczJ09QlCnJ
"""

!pip install ultralytics

import os
import shutil
import torch
from ultralytics import YOLO

from google.colab import drive
drive.mount('/content/drive')

import os
os.makedirs("/content/drive/MyDrive/YOLO11Best", exist_ok=True)

!ls /content/drive/MyDrive/

data_yaml_path = "/content/drive/MyDrive/YOLO11/data.yaml"

device = "cuda" if torch.cuda.is_available() else "cpu"
device

model = YOLO("yolo11n.yaml")

train_results = model.train(
    data=data_yaml_path,
    epochs=100,
    imgsz=640,
    batch=8,
    optimizer="Adam",
    device=0,
    workers=2,
    patience=20000,
    save_period=20,
    plots=True
)

import shutil

src = "runs/detect/train/weights/best.pt"
dst = "/content/drive/MyDrive/YOLO11Best/best_yolo11.pt"
shutil.copy(src, dst)

print("Modello salvato su Drive!")

val = model.val()
print(" METRICHE VALIDATION")
print(f"Precision        : {val.results_dict['metrics/precision(B)']:.3f}")
print(f"Recall           : {val.results_dict['metrics/recall(B)']:.3f}")
print(f"mAP50            : {val.results_dict['metrics/mAP50(B)']:.3f}")
print(f"mAP50-95         : {val.results_dict['metrics/mAP50-95(B)']:.3f}")